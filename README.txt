XAUUSD SCALP PRO + H1 ANALYSIS

Replacement:
  api/scalp.js

Primary signal:
  M5 + M15

H1:
  Added full context analysis:
  - H1 direction
  - H1 structure event
  - last confirmed swing high/low
  - H1 range high/low
  - price relative to H1 structure
  - ALIGNMENT / MIXED / OPPOSED status

H1 remains context only and does NOT block a valid M5+M15 scalp.

Entry:
  closed breakout -> M5 retest (max 3 candles) -> M5 continuation confirmation
  -> minimum 1.5R target filter.

This is a backtest-driven experimental engine, not a profit guarantee.
