name: XAU Signal Push

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  check-signal:
    runs-on: ubuntu-latest

    steps:
      - name: Check XAU signal
        run: |
          curl -f -sS "https://sn-r-xauusd-ta3j.vercel.app/api/scalp?source=github-actions"
